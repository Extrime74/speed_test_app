<h1>Результаты тестирования</h1>
<%= button_to 'Logout', destroy_user_session_path, method: :delete %>

<p>Current Admin JWT Token: <%= session[:user_token] %></p>

<table>
  <thead>
    <tr>
      <th>Загрузка (с)</th>
      <th>Выгрузка (с)</th>
      <th>User-Agent</th>
    </tr>
  </thead>
  <tbody>
    <% @test_results.each do |result| %>
      <tr>
        <td><%= result.upload_time %></td>
        <td><%= result.download_time %></td>
        <td><%= result.user_agent %></td>
      </tr>
    <% end %>
  </tbody>
</table>
